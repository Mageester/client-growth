# Axiom Orbit marketing fidelity ledger

Browser path: Playwright MCP fallback because the listed in-app Browser skill file was unavailable. Local URL: `http://127.0.0.1:8788`.

| Comparison point | Concept requirement | Rendered evidence and result |
| --- | --- | --- |
| First viewport | Quiet public showcase with direct promise, CTA, and one product fragment | 1440 screenshot preserves the direct H1, two CTAs, approved lockup, and a single evidence surface; next section begins below the fold. |
| Copy | Understandable agency language; no hype or fake proof | Exact approved H1 and plain-language category contrast render. No testimonials, logos, customer counts, or ROI claims. |
| Typography | Space Grotesk-led hierarchy with restrained technical labels | Large high-contrast display type and small mono evidence labels are consistent; controls have explicit typography. |
| Palette | `#050505`, neutral surfaces, chrome only in brand artwork | Rendered pages use the locked near-black pack. Gold/green appear only as restrained semantic signal states; UI has no metallic gradient or blue glow. |
| Brand asset | Approved Axiom Orbit artwork with generous clear space | Header/footer use `axiom-orbit-primary-transparent.png` without recoloring, glow, stretching, or reconstructed lettering. |
| Product evidence | Realistic but unmistakably illustrative | Northstar HVAC, Broken quote path, checked sources, mapped service, and agency review action are labeled `Illustrative product view` and `not customer proof`. |
| Section rhythm | Open editorial bands, lines, and varied composition | Landing alternates split editorial sections, linear loop, evidence frame, watch rows, monitoring rail, and close; product alternates chapter copy/visual placement. |
| Responsive behavior | 1440, 1024, 768, 390; no overflow | Automated checks returned zero horizontal overflow at all six captured route/viewport combinations. Mobile evidence summary was repaired from three cramped columns to full-width rows. |
| Navigation | Product, Sign in, Request access; working mobile disclosure | Link targets verified as `/product`, `/login`, `/signup`. Mobile button toggles `aria-expanded`, reveals a 154px navigation region, and Escape closes it. |
| Motion | Restrained structural motion with reduced-motion fallback | Reveals, line progression, and state emphasis are CSS-only. Emulated reduced motion reports a near-zero animation duration. |

Material mismatch fixed: the first mobile render forced Account, Signal, and Mapped service into three narrow columns. At 390px they now stack into readable full-width rows, while status/evidence remain a controlled two-column pair.

Above-the-fold copy diff: no unapproved eyebrow, proof claim, metric, testimonial, or CTA label. The implementation uses the approved H1, deck, category distinction, trust line, `Request access`, and `Explore the product`.

Intentional deviation: implementation uses the exact approved chrome raster lockup instead of ImageGen's approximate wordmark. This is required by the brand guide. Product fragments are code-native rather than flattened concept imagery.

Agency-signoff assessment: the public showcase is visually faithful to the accepted direction and the locked brand system. Browser QA found no clipping, overflow, missing assets, framework overlay, or console errors in the tested routes/viewports.
