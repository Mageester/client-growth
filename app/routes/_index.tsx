export function meta() {
  return [{ title: "Client Growth" }];
}

export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 640 }}>
      <h1>Client Growth</h1>
      <p>
        V0 scaffold. The opportunity engine lives in <code>src/</code> and is
        fully testable with fixtures at zero cost. The Clients / Services /
        Opportunities UI arrives in Phase E.
      </p>
    </main>
  );
}
