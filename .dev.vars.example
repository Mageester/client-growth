# Copy to `.dev.vars` (git-ignored) for local Worker runs and live evaluations.
# NEVER commit the real file.

AI_PROVIDER=mock

# Only needed for explicit live DeepSeek runs (AI_PROVIDER=deepseek):
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat

MAX_AI_CALLS_PER_RUN=10
